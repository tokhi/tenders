<%= content_tag(:p, notice, :class => 'success') if notice.present? %>
<table id="hor-minimalist-b" summary="companies table">
<tr>
   <th scope="col">Company</th>
    <th scope="col">Stablished.Y</th>
    <th scope="col">Busines Size</th>
    <th scope="col">Service Desription</th>
  </tr>
<% @companies.each do |company| %>
<tbody>  
<tr>
    <td><%= link_to company.org_name, company %></td>
    <td><%= company.stablished_year %></td>
    <td><%= company.business_size %></td>
    <td><%= company.service_description[0..29] %></td>
<% if logged_in? %>
<% if current_user.role == "admin" %>

<td><%= link_to 'Destroy', company, confirm: 'Are you sure?', method: :delete %></td>
<% end %>
<% end %>  
</tr>
<% end %>
</tbody>
</table>
<br />
<%= link_to 'New Company', new_company_path %>
